# Triage sheet — fresh-eyes pass 2026-07-11

**Rules for this session** (from HANDOFF/CLAUDE.md standing rules):

- 🧊 **Deploy freeze while she tests** — collect, don't hot-fix.
- 🚌 **Two buses:** `web` fixes can ship same-day (deploy gate runs E2E);
  `native` fixes batch into the NEXT EAS build, on Andrew's command only.
- 🧪 Every bug that reached a person → regression test in the owning layer +
  `docs/TEST-COVERAGE.md` update, same change.
- ✏️ Any copy change → repo-wide grep across `tests-e2e/`, `.maestro/`, and
  `locales/*/common.json` BEFORE shipping (last night's lesson).
- 🤫 "Missing Lola ack" may be the deterministic backstop firing (`?`/`¿` or >260
  chars → silently dropped) — check before filing as a bug; drop-rate metric still TODO.
- 📊 During the session: watch Sentry (new issues) + PostHog dashboard 808581
  (her interview-v2 events).

**Severity:** P0 broken/blocks · P1 wrong/misleading · P2 rough edge · C comprehension
stumble (she hesitated / misread — not a defect, still gold).

**Bus:** `web-now` · `native-batch` · `copy` (grep rule!) · `not-a-bug` · `later`

| # | Platform | Where | What happened | Expected | Sev | Bus | Shot? | Regression test layer | Status |
|---|----------|-------|---------------|----------|-----|-----|-------|----------------------|--------|
| 1 |          |       |               |          |     |     |       |                      |        |
| 2 |          |       |               |          |     |     |       |                      |        |
| 3 |          |       |               |          |     |     |       |                      |        |
| 4 |          |       |               |          |     |     |       |                      |        |
| 5 |          |       |               |          |     |     |       |                      |        |
| 6 |          |       |               |          |     |     |       |                      |        |
| 7 |          |       |               |          |     |     |       |                      |        |
| 8 |          |       |               |          |     |     |       |                      |        |

## Comprehension notes (where she hesitated, misread, or asked "what does this mean?")

-
-
-

## Blind-spot checklist (must be explicitly confirmed, not assumed)

- [ ] **Persistence:** roadmap + done-marks survive sign-out → fresh browser → sign-in (web)
- [ ] **Persistence:** web-built roadmap appears on phone after Apple sign-in
- [ ] Magic-link email: arrived fast, right language, link works
- [ ] Mid-interview language switch re-issues the current question in the new language
- [ ] Spanish surface skim (roadmap, one guide, interview start) — least-human-verified locale
- [ ] Build 35 native items: ✕ Done clears Dynamic Island · voice OFF by default (Andrew)
- [ ] PDF export: web + native
- [ ] "Something changed?" re-plan narrates a sane diff

## After the session

- [ ] Pull TestFlight beta feedback (screenshots she shared in-app) + crashes
- [ ] Sort the table into the two buses; ship web bus; queue native bus for next build
- [ ] Regression tests + TEST-COVERAGE.md for everything fixed
- [ ] Homework pages (`app/how-i-was-built/log.tsx` + `roadmap.tsx`) ride the fix batch
- [ ] Fold results into HANDOFF.md resume block
