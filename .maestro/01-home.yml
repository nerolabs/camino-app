# P0: the app launches and the home screen renders.
appId: ${APP_ID}
---
- launchApp:
    clearState: true
- extendedWaitUntil:
    visible: "Build my free roadmap.*"
    timeout: 45000
- assertVisible: "Get Camino.*"
- assertVisible: ".*sample roadmap.*"
