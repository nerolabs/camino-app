import SamplePlanScreen from '@/components/SamplePlanScreen';

// /sample-plan — the default persona (Susan & Tom). The other personas live at
// /sample-plan/[persona]; the shared screen is components/SamplePlanScreen.tsx.
export default function SamplePlanIndex() {
  return <SamplePlanScreen />;
}
